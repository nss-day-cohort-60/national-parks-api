class Photos():
    """Defining the photos class"""
    def __init__(self, id, url, user_id, park_id):
        self.id = id
        self.url = url
        self.user_id = user_id
        self.park_id = park_id
